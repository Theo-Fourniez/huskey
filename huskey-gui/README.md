# Huskey GUI
## Quickstart

### Use the right node version
The version is specified in the .nvmrc file and you can use [nvm](https://github.com/nvm-sh/nvm) :

`nvm use`

### Install deps
`npm install`

### Run the dev server
`npm run tauri dev`

## Building it
`npm run tauri build`